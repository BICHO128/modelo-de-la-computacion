{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Simulation",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/run_sim.py",
            "args": ["--num", "30", "--pasos", "30"],
            "console": "integratedTerminal"
        }
    ]
}
